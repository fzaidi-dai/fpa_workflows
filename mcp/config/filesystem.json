{
    "server": {
        "port": 3001,
        "host": "0.0.0.0"
    },
    "filesystem": {
        "allowedDirectories": [
            "/mcp-data/data",
            "/mcp-data/scratch_pad",
            "/mcp-data/memory"
        ],
        "readOnly": false
    },
    "logging": {
        "level": "info",
        "format": "json"
    }
}
