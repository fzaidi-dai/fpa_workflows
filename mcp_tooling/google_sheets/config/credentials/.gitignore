# Ignore all credential files to prevent accidental commits
# NEVER commit credentials to version control!

*.json
*.key
*.pem
*.p12
service_account.json
credentials.json
token.json
*_key.json
*_credentials.json

# Allow this .gitignore file and README
!.gitignore
!README.md